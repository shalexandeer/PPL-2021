let tipeData = (A = 13, B = 2, C = "Pengantar Pemrograman dan Logika") => {
    console.log(`Nilai A = ${A}`);
    console.log(`Nilai B = ${B}`);
    console.log(`Hasil Bagi A dan B = ${Math.floor(A/B)}`);
    console.log(`Nilai C = ${C}`);
}
tipeData();